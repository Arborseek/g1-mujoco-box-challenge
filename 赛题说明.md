# 赛题详情：宇树 G1 人形机器人搬箱仿真

**文档版本：** v1.1  
**适用平台：** MuJoCo · 宇树 G1（含灵巧手）  
**配套工程：** 本仓库（`contest/`）

---

## 一、赛题说明

### 1.1 任务是什么

本赛题只有一个核心目标：

> **控制宇树 G1 人形机器人，将拾取台上的 10 个箱子搬运至右侧放货区的放置桌面并完成放置。**

形式化表述：设箱体集合 \(\mathcal{B}=\{0,1,\ldots,9\}\)，成功放置集合 \(\mathcal{S}\subseteq\mathcal{B}\)。任务完成当且仅当：

\[
|\mathcal{S}| = 10
\]

除「搬完 10 箱」外，**不设额外考核指标**（不限定行走方式、抓取手法、路径形态、耗时或摔倒次数等）。参赛队可在官方基线上自由优化、替换或新增模块。

### 1.2 官方基线的定位

本仓库提供一套**可运行的基础实现**，包括：

- MuJoCo 仿真环境与场景；
- 预训练 RL 双足行走策略；
- 示例状态机（够取 → 抱箱 → 通道搬运 → 放货）；
- 手动遥操作与可视化调试工具。

基线**不是标准答案**，也**不代表最优方案**。其作用是：

1. 降低环境部署门槛（`bash setup.sh` 即可运行）；
2. 提供 `BoxPickPlaceEnv` 等公开 API，供上层算法调用；
3. 给出一种可行的搬箱流程参考，便于在此基础上改进。

参赛队可以：

- 修改 / 替换行走策略、状态机、抓取与上肢控制；
- 在 `teams/<队名>/` 下新增规划、感知、恢复等模块；
- 调整 `config/task.yaml` 中的非场景固有参数（如路点、阈值、姿态）；
- 完全重写控制器，仅复用环境与场景。

---

## 二、场景与对象

### 2.1 布局示意

```
                    ┌──────────────────────┐
                    │     放货桌面          │
                    │   放置区 D (x≈3.5m)   │
                    └──────────────────────┘
                              ↑
                    · · · · · │ 南侧通道 (y≈-0.90m)
                              │
    ┌─────────────┐             │
    │  拾取工作台   │             │
    │  10 箱 2×5  │←—— G1 出生点 (-0.78, 0)
    └─────────────┘
         拾取区 P
```

| 要素 | 参数 |
|------|------|
| 机器人出生点 | \((-0.78,\,0,\,0.79)\,\mathrm{m}\)，偏航 \(0°\) |
| 拾取区 | 中心 \((0.15,\,0,\,0.85)\,\mathrm{m}\)，10 箱 \(2\times5\) 阵列 |
| 放置区 | 中心 \((3.5,\,0,\,0.85)\,\mathrm{m}\)，半尺寸 \((0.45,\,0.45,\,0.12)\,\mathrm{m}\) |
| 放货桌面高度 | \(0.73\,\mathrm{m}\) |
| 仿真步长 | \(\Delta t = 0.002\,\mathrm{s}\) |

### 2.2 对象规格

| 对象 | 规格 |
|------|------|
| 机器人 | 宇树 G1 + 灵巧手 |
| 箱体 | 边长 \(0.24\,\mathrm{m}\)，质量 \(0.05\,\mathrm{kg}\) |

### 2.3 参考作业流程

基线采用的流程如下，**非强制要求**，参赛队可自行设计：

```
导航至拾取位 → 够取/抓取 → 搬运 → 放置 → 返回 → 下一箱
```

---

## 三、放置判定

箱体 \(i\) 被环境标记为「已放置」，当且仅当：

\[
\begin{cases}
|\,p_{i,x}-c_x\,|\le h_x,\;\;|\,p_{i,y}-c_y\,|\le h_y,\;\;|\,p_{i,z}-c_z\,|\le h_z+\delta_z \\[4pt]
\|\mathbf{v}_i\|_2 < v_{\mathrm{th}} \\[4pt]
p_{i,z} \ge h_{\min}
\end{cases}
\]

| 参数 | 取值 |
|------|------|
| 放置区中心 \((c_x,c_y,c_z)\) | \((3.5,\,0,\,0.85)\,\mathrm{m}\) |
| 放置区半尺寸 \((h_x,h_y,h_z)\) | \((0.45,\,0.45,\,0.12)\,\mathrm{m}\) |
| 速度阈值 \(v_{\mathrm{th}}\) | \(0.15\,\mathrm{m/s}\) |
| 最低高度 \(h_{\min}\) | \(0.80\,\mathrm{m}\) |

持箱过程中不参与判定；判定逻辑见 `src/env/task.py`。

---

## 四、开发与扩展

### 4.1 控制器接口

在 `src/controllers/base.py` 中定义了统一接口，便于替换基线：

```python
from src.controllers.base import TeamController
from src.env import BoxPickPlaceEnv

class MyController(TeamController):
    def reset(self, env: BoxPickPlaceEnv) -> None:
        ...

    def step(self, env: BoxPickPlaceEnv) -> None:
        ...
```

主循环：`controller.reset(env)` → 循环 `{ controller.step(env); env.step() }` → 直至 \(|\mathcal{S}|=10\) 或达到步数上限。

推荐从 `teams/template/controller.py` 复制起步。

### 4.2 工程目录结构

```
contest/
├── config/
│   ├── task.yaml                   # 任务 / 行走 / 抓取参数
│   └── locomotion_g1.yaml          # RL 行走策略配置
├── assets/                         # 机器人模型与场景 XML
├── third_party/g1_policy/          # 预训练行走权重
├── src/                            # 官方基线核心代码
│   ├── core/                       # 配置加载
│   ├── env/                        # 仿真环境、抓取 API、放置判定
│   ├── control/                    # RL 行走、PD 力矩、速度辅助
│   ├── controllers/                # 基线状态机、手动模式、TeamController 接口
│   ├── planning/                   # 【预留】路径 / 站位规划
│   ├── manipulation/               # 【预留】抓取 / 上肢操作
│   └── utils/                      # 【预留】工具函数
├── teams/                          # 参赛队扩展代码（主要开发位置）
│   ├── template/controller.py      # 起步模板
│   └── <队名>/                     # 各队自行创建
└── scripts/
    ├── run_sim.py                  # 可视化运行（手动 / demo）
    ├── evaluate.py                 # 无头运行，输出完成情况
    └── tools/                      # 辅助调试脚本
```

#### 扩展建议

| 模块 | 基线现状 | 可优化方向（示例） |
|------|----------|-------------------|
| `src/control/` | RL 行走 + 速度辅助 | 换策略、改 cmd 生成、持箱步态微调 |
| `src/controllers/` | 有限状态机 | 换调度逻辑、并行规划、失败重试 |
| `src/planning/` | 空（预留） | 拾取站位、通道路点、动态避障 |
| `src/manipulation/` | 空（预留） | 够取姿态优化、力控抓取、放货策略 |
| `teams/<队名>/` | 队伍私有代码 | **主要提交与迭代位置** |

### 4.3 环境部署与运行

需预先安装 [Conda](https://docs.conda.io/)（Miniconda / Anaconda）。若未安装，执行 `bash setup.sh` 会**打印安装指引**；也可使用：

```bash
bash setup.sh --install-miniconda   # 自动安装 Miniconda 到 ~/miniconda3
```

```bash
bash setup.sh
conda activate unitree_pick_place

# 可视化：官方基线自动搬箱
python scripts/run_sim.py --demo

# 可视化：手动遥操作调试
python scripts/run_sim.py

# 无头运行：官方基线
python scripts/run_sim.py --demo --headless

# 无头运行：加载自定义控制器
python scripts/evaluate.py --controller teams/my_team/controller.py
```

### 4.4 基线技术概要

- **行走：** `unitree_rl_gym` 预训练策略 + PD 力矩控腿；
- **抓取：** 手腕距离判定 + 运动学跟手；
- **搬运：** 双臂抱箱（cradle）同步箱位；
- **放货：** 上肢放货姿态 + 释放。

算法细节见 [README.md](README.md)。

---

## 五、参赛代码组织（参考）

```
teams/<队名>/
├── controller.py       # 导出 TeamController
├── planning/           # 可选
├── manipulation/       # 可选
└── README_team.md      # 算法说明（建议）
```

```bash
cp -r teams/template teams/my_team
# 编辑 teams/my_team/controller.py 后：
python scripts/evaluate.py --controller teams/my_team/controller.py
```

---

## 六、常见问题

**Q：必须沿用基线状态机吗？**  
不必。只要通过 `TeamController` 接口驱动环境，内部实现完全自由。

**Q：可以改行走策略吗？**  
可以。可继续使用官方 `motion.pt`，也可替换为自研策略。

**Q：必须按南侧通道走吗？**  
不必须。通道是基线参考路径，参赛队可自行规划。

**Q：摔倒了怎么办？**  
基线手动模式提供 `U` 键起身；自动模式下可自行实现恢复逻辑。赛题不对摔倒做额外要求。

**Q：完成判定可以改吗？**  
放置判定由环境 `src/env/task.py` 统一执行，保证「搬入放置区」语义一致。场景 XML 与放置区参数应与官方一致。

**Q：手动模式和自动模式区别？**  
手动模式（`run_sim.py`）供开发调试；自动模式通过 `controller.step()` 无人值守运行。

---

## 附录：关键文件

| 文件 | 用途 |
|------|------|
| `config/task.yaml` | 场景与任务参数 |
| `src/env/box_pick_place.py` | 仿真环境 |
| `src/env/task.py` | 放置判定 |
| `src/controllers/demo.py` | 官方基线 |
| `src/controllers/base.py` | 控制器接口 |
| `scripts/run_sim.py` | 可视化入口 |
| `scripts/evaluate.py` | 无头运行入口 |
| `teams/template/controller.py` | 扩展模板 |

---

*本仓库为仿真比赛配套基线工程，赛题核心目标为完成 10 箱搬运，其余实现细节开放优化。*
